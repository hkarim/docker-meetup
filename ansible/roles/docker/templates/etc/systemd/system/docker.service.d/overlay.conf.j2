[Service]
ExecStart=
ExecStart=/usr/bin/docker daemon -H fd:// --cluster-advertise {{network_device}}:2376 --cluster-store consul://{{hostvars[ansible_hostname][network_interface].ipv4.address}}:8500
